// Implement a function which convert the given boolean value into its string representation.

// Note: Only valid inputs will be given.

const booleanToString = boolean => boolean ? 'true' : 'false'


/*
Create a function the converts a boolean into a string. The string will return either 'true' or 'false'
Returning the string of 'true' or 'false'
Example: true => 'true'  false => 'false'
Create an arrow function with a paramenter of string(true/false)
    1. create a ternary to check if it is Boolean
        if so then return 'true'
        else return the 'false'
*/

