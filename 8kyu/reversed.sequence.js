// Build a function that returns an array of integers from n to 1 where n>0.
    const reverseSeq = n =>{
        let arr =[]
        for(let i =n; i > 0; i--){
            arr.push(i)
        }
        return arr
    }

/*
Create a function that return intergers from the paramerter starting with the greatest numbers 
Returning an array with the numbers the count up towards the paramenter starting with largest number ending in one 
Create an arrow function reverseSeq that takes on param (number)
Create ann array that hold an empty array 
    let arr=[]
for loop to loop up until the given paramente but we will start the parameter with 1 
    for(let i = number; i >= 1; i--)
    push the number into the empty array 
    arr.push(i)
return the arr 
*/

