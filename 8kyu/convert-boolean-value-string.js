// Complete the method that takes a boolean value and return a "Yes" string for true, or a "No" string for false.

const boolToWord = bool => bool ? 'Yes' : 'No'



/*
Create a function that takes a boolean return 'Yes' for true and 'No' for false
Returning a string of 'Yes' for true and 'No' for false 
Example true => 'Yes' / false => 'No'
1. Create a an arrow function boolToWord with one parameter 
    if argumenet is === true  
    then return 'Yes'
    else return 'No'
*/

